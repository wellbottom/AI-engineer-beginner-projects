# Placeholder so git tracks the empty services/ directory.
# services/ holds the Python FastAPI backend services (one per mini-project
# that needs server-side logic). Remove this file once a service exists here.
